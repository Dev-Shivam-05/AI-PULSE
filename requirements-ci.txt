# Lean dependencies for the free GitHub Actions runner (no GPU/torch).
requests>=2.31
Pillow>=10.2
kokoro-onnx>=0.4
soundfile>=0.12
edge-tts>=6.1.9
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1
faster-whisper>=1.0
opencv-python-headless>=4.9,<5
rembg>=2.0
# v3-B tool-video visuals: screen recording + code cards.
# playwright is pinned EXACTLY: the CI browser cache key hashes this file, and a
# floating pin would keep serving a stale Chromium after a playwright release.
playwright==1.60.0
pygments>=2.17
# v3-C: the free 1-page cheat-sheet PDF per tool video
reportlab>=4.0
