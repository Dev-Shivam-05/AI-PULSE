# ---- FactVerse Python dependencies ----
# Install:  py -3 -m venv .venv && .\.venv\Scripts\python -m pip install -r requirements.txt

# Core
requests>=2.31

# Media / imaging
Pillow>=10.2

# Voice: Kokoro (default, free/Apache, near-real-time CPU) + edge-tts fallback
kokoro-onnx>=0.4
soundfile>=0.12
edge-tts>=6.1.9

# Word-accurate caption timing (local whisper alignment)
faster-whisper>=1.0

# YouTube (official Data API v3)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1

# Instagram (unofficial fallback; official Graph API uses requests)
instagrapi>=2.0

# Dev / tests
pytest>=8.0

# Thumbnail person-frame mining (face detection)
opencv-python-headless>=4.9,<5

# Creator-style thumbnail cutouts (background removal)
rembg>=2.0
